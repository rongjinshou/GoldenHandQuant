# 当前进展速览（2026-06-11 更新）

## 我们在做什么

一句话：**给 ¥146k 实盘账户找到第一个经得起验证的 edge**。

路线是"验证优先"：先把数据和测试引擎修到诚实，再让因子假设过 §7 硬门槛，
活下来的才进纸面前向 → 小资金 → 全自动。

| 阶段 | 内容 | 状态 |
|---|---|---|
| Phase 0 | 数据地基 + 诚实因子测试引擎 | ✅ 完成 |
| Phase 1 | P0 五因子过硬门槛漏斗 | 🔄 第一轮已跑：**0/5**，修 bug 后重跑 |
| Phase 2 | 幸存因子纸面前向 | |
| Phase 3 | 小资金半自动实盘 | |
| Phase 4 | 全自动 | |

## 第一份诚实判决已出（2026-06-11）

全 A 股 5207 只 × 1212 交易日 × 扣成本 × IS/OOS 切分：**0/5 通过**。
详见 `2026-06-11-p0-verdict-report.md` + `2026-06-11-p0-verdict.json`。要点：

- 五因子 OOS IC 符号全部与 IS 一致、单调性近满分——排序信息真实，不是幻觉
- 但没有一个能把排序转化成扣成本多空收益：F01/F02/F03 卡 IR<0.3，
  F04/F05 卡 OOS 多空 ≤0，F03 中性化后仅 0.005（小市值影子）
- F04 低波动最接近过线（88/A，仅 OOS 多空一项不过）

**发现引擎 bug**：`return_20d` 用 `closes[0]`（≈119 日收益）而非 `closes[-21]`。
F02 实测为半年反转；中性化控制变量同错 → F03/F04/F05 的正交化判定需重验。

## 已完成（按 commit）

- `61ecc7e` QMT 缓存覆盖判定修复 + 放开全市场 5208 只股票池
- `0502498` 因子引擎三处正确性修复 + 判决收紧（有符号 IC、单调性 ≥0.6）
- `e1ea872` 因子假设库设计文档：12 张因子卡
- `9410e5d` `--rebalance-days N`：分层回测可配置调仓间隔
- `218228e` QMT 缓存 start 侧缺口判定 + 取数异常不再静默吞
- （工作区）`prepare_snapshots` 特征化测试补完：stub fetcher + 7 个测试钉死
  无前视 / T-1 信息 / warmup 三个约定，全量 suite 绿

## 进行中：market-data-store（设计已定，实施中）

用户已拍板两个架构决定：

1. **打破 domain 红线**：允许纯计算库（numpy/pandas/scipy），仍禁 I/O/SDK/框架
2. **市场数据入库**：DuckDB（`data/market.duckdb`），五张表
   （instruments/bars/fundamental_snapshots/stock_features/fetch_meta），
   多源 source 列，履约区间只刷缺口，特征版本化

含向量化特征引擎（修 return_20d，golden test 对照手写版），落地后特征构建
从 ~2 小时降到分钟级。设计与实施计划见 `docs/feat/0611-market-data-store/`。

## 下一步（最重要的一件事）

market-data-store 落地后，用修复口径重跑 P0 判决（含真正的 20 日反转 +
正确的中性化控制），重点看 F04 低波动；命令同前但走数据库快路径：

```bash
WIN_PYTHON="/mnt/c/Users/11492/.conda/envs/goldenhandquant/python.exe"
$WIN_PYTHON -m src.interfaces.cli.quant factor-test --factors P0 \
  --start-date 2021-01-01 --end-date 2025-12-31 \
  --split-date 2024-06-30 --rebalance-days 5
```

## 环境备注

- superpowers 插件已装（`superpowers@claude-plugins-official` v5.1.0，user 级），
  本会话中途安装故未注册，下次会话生效
- duckdb 已装入 Windows conda env（goldenhandquant）
