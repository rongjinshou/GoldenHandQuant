# 全闭环 v1 · 夜审报告（2026-06-11 晚）

委托范围：整体 review 代码/方案/架构 + 巩固系统 + 研究主线推进。
评审方法：7 个独立查找角度（逐行扫描 / 被删行为审计 / 跨文件追踪 / 复用 / 简化 / 效率 / 实现高度）
并行扫 `f5f94d3...HEAD`（白天交付的 41 文件 +3075/-801），38 个候选去重核验后按严重度处置。
修复全部走 TDD，commit `a59e147`，全量测试绿 + ruff 干净。

## 一、已修复（当晚，按严重度排序）

| # | 发现 | 后果（若不修） | 修复 |
|---|---|---|---|
| 1 | **SQLite 连接绑定主线程**：store/审计在主线程创建，守护循环在调度线程调用，每次写入抛 `ProgrammingError` 且被调度器吞掉 | **明早 09:35 守护循环静默全失败**——零下单、零留痕、进程看似正常。`--once` 与全部单测同线程，测不出 | `Database` 开 `check_same_thread=False`（单写者串行访问安全），加跨线程回归测试 |
| 2 | **调度器漏触发/双触发**：`sleep(60)`+精确 `HH:MM` 匹配，采样相位漂移跨过整分钟即漏掉当日执行时刻；`check_interval<60` 时双触发 | 早盘循环随机性静默缺席 | 槽位追踪：「时刻已到且今日未触发」即补发；启动时预标记已过时刻；周末不再空转 |
| 3 | **单笔异常炸穿循环**：行情断连等异常传出 `run_cycle`，后续候选全跳过、快照与 finalize 不执行 | 孤儿循环记录 + 当日亏损闸因无快照而全天失效 | 逐单 try 隔离为 FAILED 留痕；`_snapshot`/`_finalize` 移入 `finally` 必达 |
| 4 | **亏损禁买闸基准缺失**：基准=当日首快照，但快照只在循环末写——首循环永远无基准（fail-open），且基准被首循环交易污染 | 2% 回撤保护在最需要的日子（隔夜跳空）整天失效 | 当日首循环开始时先落**盘前基准快照**再交易 |
| 5 | **CANCELED 不占预算**：QMT 部成部撤（`ORDER_PART_CANCEL`）映射为 CANCELED，已成交敞口不计入日预算与当日去重 | 同标的同方向当日重复追单，实际敞口突破 ¥3000 日上限 | `CANCELED` 加入 `_BUDGET_STATUSES` |
| 6 | **卖出零股被拒**：`volume%100` 检查误覆盖卖出侧——A 股送配产生的零股允许一次性卖出 | 残股形成只能人工清的死仓 | 整数倍限制只约束买入 |
| 7 | **dry 单号进程重启撞主键**：`count(1)` 计数器使次日的 `dry-000001` `INSERT OR REPLACE` 覆盖昨日留痕 | 纸面前向历史账本被静默换血，前向验证统计失真 | 单号改 uuid，附跨实例唯一性测试 |
| 8 | **同循环资金闸用陈旧快照**：多笔买单共用循环开头的 `available_cash`，前单冻结不扣减 | live 下后续单靠券商拒单兜底；dry-run 下记录真实资金支撑不了的成交序列，**前向数据失真** | 循环内资金游标逐单扣减 |
| 9 | **日级防线按 mode 切分**：dry_run/live 背后是同一真实账户，切换模式当日即重置预算/去重/亏损基准三道防线 | 上午纸面+下午实盘的组合一次击穿三道日防线 | `today_*`/`day_start_equity` 改跨 mode 统计 |
| 10 | **mode 与网关可任意失配**：service 不校验，绕过 CLI 装配可造成「标注 dry_run 实际真单」 | 审计灾难：真金白银出账但留痕全是 dry_run | 网关声明 `is_dry_run`，构造时强制配对校验 |
| 11 | **报价新鲜度闸名存实亡**：只查 `last>0`，停牌/断连回退的陈旧快照可通过全部闸 | 按几小时前的价格挂真单 | 超 180s 拒单（`MAX_QUOTE_AGE_SECONDS`） |
| 12 | P1 快赢一组 | — | 配置加载兜底退出；`synchronous=NORMAL`（/mnt/c 上 fsync 代价）；live 路由 async→def（不阻塞事件循环）；权益曲线按 mode 分线（消锯齿假象）；ticket price_band 回归 `PRICE_BAND` 单源；删 `strategy_names` 死字段 |

## 二、登记为债（有意不当晚修，按价值排序）

| 项 | 内容 | 建议时机 |
|---|---|---|
| D1 | **轮询逻辑两份**（ticket `_poll_until_terminal` vs auto `_poll`，超时行为已分叉：前者不撤单后者撤单） | 下次触碰任一处时抽共享 helper |
| D2 | **执行留痕用裸 dict** 而非扩展 `ExecutionRecord` VO；状态词表散落 4 处（qmt 映射/`_TERMINAL`/`_BUDGET_STATUSES`/app.js）| 独立小 Spec：状态枚举收敛 + VO 化 |
| D3 | **调度器时段与闸时段两套定义**（9:25 vs 9:30）：闸是强制点，调度器宽松无害，但应收敛到 domain `check_trading_session` | 与 D1 同批 |
| D4 | **逐单 `subscribe_first_tick` 串行最坏 3s/单**：应循环开始批量 `get_quotes` + 失败标的才走订阅 | live 启用前 |
| D5 | **驾驶舱效率**：`loadLive` 5s 全量拉 5 端点全量重渲染；`/backtests` 全量携带净值曲线 | 数据量上来后（runs>30）|
| D6 | **`check_symbol_scope` 白名单与现实有出入**：002xxx 自 2021 并板后属深主板（10% 带）但被拒；板块带宽 `band` 形参未穿透 | 扩标的范围时配置化一并做 |
| D7 | **`test_factor_test_cli` 直连真实 `data/market.duckdb`**：与任何写进程（refresh/factor-test）并发即假失败 | 测试隔离修复（注入 tmp db）|
| D8 | LiveSignalService 被注入 trade_gateway 但 scan 路径不用；`ITradeGateway` 协议缺 `query_order_status/cancel_order` | 与 D2 同批补协议 |
| D9 | 旧编排链（AutoTradingEngine/TradingOrchestrator/SignalPipeline）旁路保留 | 闭环跑稳后归档/合并 Spec |

## 三、明确不修（评审后裁定）

- **周末拒单文案变化**（「非连续竞价时段」→「非交易日」）：新文案更准确，下游无文案解析依赖。
- **`--enable` 语义与旧帮助文案不同**：三重确认是设计决策（DD-2），帮助文案已同步改为「仅 dry-run 生效」。
- **驾驶舱 positions 端点不按 mode 过滤**：快照行自带 mode 字段可见；双模式并行运行属罕见形态，等 D5 一并处理。

## 四、当晚系统运行记录

- 18:32 守护进程首次上线（QMT 连接成功）→ 19:08 因夜审发现 #1 主动停机
- 18:50–19:15 `data refresh`：bars 缺口补到 **2026-06-11**（+54 万行），基本面快照 0→654 万行，
  **特征全量重算**（修复版引擎 v1，5207 只到今日）——研究数据彻底就绪
- 19:09 重启失败一次（refresh 收尾争用交易端连接，瞬时），19:11 修复版守护进程上线 ✅
- 19:1x 起研究批次后台串行：P0 重判（修复特征+窗口延至 2026-06）→ P1 首测 → F04 调仓敏感性（1/10/20 日）
  ——判决自动入库 `factor_verdicts`，晨间驾驶舱可查

## 五、方案/架构层结论

白天交付的闭环架构（LiveSignalService 脊柱 + domain 共享闸 + 双库留痕 + 只读驾驶舱）**方向正确**，
夜审未发现需要推翻的结构性决策；发现集中在「守护进程长跑形态」与「防线语义边界」——
这正是 dry-run 纸面前向阶段存在的意义：**让这些缺陷在零成本环境里暴露**。
本次 11 项修复后，明早 09:35 的首个自动循环已具备可信运行条件。
