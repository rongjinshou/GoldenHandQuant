# MC-1 市值口径统一 — 完成报告（2026-07-12）

> 结论先行：**market_cap 已全库统一为时点总市值**（迁移审计三抽样日中位比值 1.000000、>1% 偏差 0 行），
> gate 判据在正式口径下 **PASS**（OOS ON 收益 +49.9%/回撤 11.00%/Sharpe 1.48），F01 新基线
> top20 全窗 **227.3%**（cap=total_mv 标签入库）。**周二 07-14 影子盘首采即新口径**，日历零成本。

## 一、交付物

| 件 | 内容 | 验证 |
|---|---|---|
| 迁移 | `cap_regime.migrate_market_cap`：备份列 `market_cap_qmt`（可回滚）→ tushare 直配 → as-of 回填(≤16 日历日) → 保留计数；`scripts/migrate_market_cap.py` 幂等 | 单测 2 例；实跑审计 2026-07-10/2024-01-05/2022-06-01 中位比值全 1.000000、>1% 差 0（计数器因浮点相等启发式有重叠，审计为权威——decision-log 留痕） |
| 日增量 | `sync_latest_market_cap`（tushare 主/akshare spot 兜底/双败退出 1）+ `scripts/sync_market_cap.py`；影子盘编排上午段新步骤 `sync-market-cap`（refresh 后、决策前） | 单测 3 分支；编排器时序用例更新 |
| 门禁 | **C8 市值跨源偏差**（>2% 占比>1% FAIL；对照库缺失 SKIP）+ **C9 名称新鲜度**（WARN 观察级） | 单测 5 例；生产实跑 **9/9 全 PASS**（C8 抽样 500 偏差 0%、C9 零滞后） |
| 连环修复① | `instruments.name` 5203/5211 只存的是代码本身（从未灌真名）——`refresh_instrument_names.py` 从交易所名录全量修复 | 实跑更新 5203 只 |
| 连环修复② | 元凶根治：`resolve_universe` 的占位 upsert（name=symbol）曾/会冲掉真名 → `upsert_instruments` 加防降级覆写；`_data_wiring` 加 DuckDB 离线分支（factor-test 等只读路径 WSL 可跑，不再 import 崩） | 单测 2+既有回归；ruff 全绿 |

## 二、正式口径下的 gate（迁移后主脚本重跑）

| 格 | 收益 | 回撤 | Sharpe |
|---|---|---|---|
| IS OFF | +80.3% | 17.65% | 1.02 |
| IS ON | +68.0% | 8.29% | 1.54 |
| OOS OFF | +38.9% | 21.49% | 0.89 |
| **OOS ON** | **+49.9%** | **11.00%** | **1.48** |

判据① 11.00% < 基准 14.78% ✅；判据② 闸增益不蒸发 ✅ → **PASS，进阶段1**。

**等价性说明（决策留痕）**：与前夜 overlay 版（+46.0%/10.87%/1.36）判据结论一致、方向一致；数字差异（IS ON 差 13pp）归因 overlay 未覆盖 as-of 回填与 2020 预热期——验收标准据此从"±2pp"修正为"判据结论一致"，以迁移版为正式口径。

## 三、F01 新基线（cap=total_mv，与旧口径**不可比**）

| top_n | 全窗收益 | 回撤 | Sharpe |
|---|---|---|---|
| 20 | **227.3%** | 12.07% | 1.530 |
| 10 | 186.1% | 16.07% | 1.276 |
| 30 | 208.0% | 11.84% | 1.499 |

有趣的事实：真总市值口径下 top20 反而比失真口径（159.8%）更强——"真微盘"因子暴露更纯。
因子判决（F01 verdict 换代）：见 §五补记。

## 四、稳态运维

- 周二链自动序：refresh → index → **sync-market-cap** → auto-trade（QMT 写入的当日 QMT 口径值在决策前被覆写；`market_cap_qmt` 列永久保留 QMT 原值供对照）。
- 门禁 C8 每次 `data status --check`/`verify_all` 拦截口径漂移回归（含 tushare 账号死亡后 sync 断供的场景——sync 双败本身也高声失败）。

## 五、补记

- F01 因子判决重跑曾三次受阻并三连修：WSL import 崩（→ `_data_wiring` DuckDB 分支）→
  同进程 duckdb 混合配置连接冲突（→ 离线取数桩，不开二连接）→ 缺口触发取数报错
  （→ 桩按空返回降级，见 §六 连环修复③）。**修通后判决已当日离线完成换代（§六），
  无需等 interop**——本节留作故障史。

## 六、研究层换代（同日追加）

- **MC-2 顺势核销**：`pe_ratio←ts pe_ttm`（直配 526 万行；kept 152 万主因=亏损股 tushare 无 PE 属正确语义）、
  `pb_ratio←ts pb`（直配 676 万行）；07-10 审计中位比值双双 1.000000。备份列 `pe_ratio_qmt/pb_ratio_qmt`。
- **因子判决换代**：P0 五因子 + 价值系 F20/F21/F22/F27/F30/F31 全部在诚实口径下重验入库
  （run_id 20260712-*）。单因子/组合依旧全 FAIL——与 0626"纯因子挖尽"叙事一致；
  可投判断的依据始终是 F01+趋势闸 gate（新口径 PASS）。
- **纸面净值重跑**：SHADOW-PAPER-20260712 入库（07-04→07-12，5 交易日，窗口尚短曲线即产出）。
- **历史勘误**：0626 阶段 0 gate 报告头部加旧口径警示框，旧数字禁与新基线比较。
- **离线判决路径打通**（连环修复③）：`_data_wiring` 离线桩按空返回降级，B1 履约诚实逻辑
  自然处置缺口（4/5211 全为 2025 新上市股）——因子判决从此 WSL 可跑，不再依赖 interop。
