# 回测/实盘决策核心归一 — 重构完成报告（2026-06-28）

> 主体完成。回测与实盘自此**共用同一决策核心** `CrossSectionalStrategyRunner.evaluate`，差异只在执行层；信号一致性是**架构保证**而非巧合。

## 一、阶段成果（全 commit main）

| 阶段 | commit | 成果 |
|---|---|---|
| R1 | `c77d890` | `ITradeGateway` 补 `is_dry_run`/`query_order_status`/`cancel_order` 契约 + Mock/QMT/DryRun 实现；去 `auto_trade` getattr duck-typing；Mock 可跑实盘循环 |
| R2 | `ac74563` | `StrategyRunner` 确立为回测/实盘共享决策核心（核实其已中立、只依赖 Protocol、提纯≈0）+ module docstring + golden 基线 |
| R3a | `fd89097` | 归档僵尸 `AutoTradingEngine`/`TradingOrchestrator`/`SignalPipeline`(+3 测试)；保留 `trading_scheduler`（auto_trade 守护真用）；**债 D9 关闭**；实盘只剩 `AutoTradeAppService` 一套编排 |
| **R3b** | `f058f4f` | **核心**：`LiveSignalService._scan_cross_sectional` 改用 `CrossSectionalStrategyRunner.evaluate`（方案 Y）+ `OrderTarget→SignalDisplay` 转换；删 `_build_fallback_snapshots`（market_cap=0 降级根源）；实盘截面接趋势闸/基本面装配/批量 sizer |
| R4 | `e8bbe22` | 信号一致性守门：`test_scan_signal_consistency_with_backtest_runner` 验证实盘 `scan` 逐位 == 回测 `runner.evaluate` |

## 二、方案 Y（对 design DD-4 的优化）

design 原设想「改 `AutoTradeAppService` 消费 OrderTarget」，实施时优化为**方案 Y**：改 `LiveSignalService.scan` 内部用决策核心，`AutoTradeAppService` 零改动 → 避免所有 OrderTarget 适配缺口（confidence/字段名/direction）、两条实盘路径（全自动+半自动）一处统一。`risk_settings` 用决策核心默认（index_symbol=000852.SH）、`settings` 都不用改。

## 三、验证

- 每阶段 **Workflow 实施 + 3 维对抗验证**（回归审计 / 接口或决策核心正确性 / 边界守护）。
- **golden 守门**：全套回归逐阶段绿（R3a 后 1298 基线，R3b/R4 后 0 failed/0 error）、ruff 干净。
- 回测集成测试（test_integration_backtest / test_micro_value_integration / test_strategy_runner_lookahead）逐位绿 → 决策核心行为对回测不变。
- **R4 端到端一致性**：相同 market/fundamental/sizer/日期下，实盘 `scan` 与回测 `evaluate` 目标组合逐位相同。

## 四、剩余债收口（2026-06-30 续做，三笔全处理）

| 债 | 处置 | commit |
|---|---|---|
| **R3c 线上 wiring** | 抽 `build_live_signal_service`(`_auto_trade_wiring.py`)按 `strategy_type` 分流：cross_section → `EqualWeightSizer(top_n)` + `build_backtest_cross_section("DuckDBHistoryDataFetcher")` 离线全市场 registry + 宇宙(at.symbols 非空取交集)；bar → 原 `FixedRatioSizer`+`at.symbols`。脱 QMT 单测 3 例。`auto-trade --once dry_run` 的 cross_section 决策核心不再因 `fundamental_registry=None` 静默空跑 | `5c9091b` |
| 时序 `_scan_bar` 统一 | **裁定不强行统一**：强并会回退 live 质量（限价用 T 开盘=回测产物、抹掉 per-signal confidence 破坏 `min_confidence` 闸、丢 reason），而决策本就同源（同 `strategy.generate_signals` + sizer），非截面那种决策分叉。留档为正当执行层边界 + 写明触发统一条件 | `ff55e25` |
| 换行符 | 全仓 CRLF→LF（106 `.py` + 16 其它）+ `.gitattributes`(`text=auto eol=lf`)防复发；`git ls-files --eol \| grep -c crlf` = 0 | `0ed8ae8` |

**仍属真单 Spec（不在本批）**：真实 QMT live 的**实时 fundamental 装配** —— 本批 dry-run/影子盘走 DuckDB 同源；真账户实时基本面 + QMT 行情时间对齐属真单 Spec 范畴。

## 五、与主线的关系

F01 影子盘从 `0626-mainboard-f01-shadow` 阶段 1 **升级为本重构 R4**；`0626` 阶段 0 gate（主板域 F01+趋势闸 PASS，OOS Sharpe 1.69）成果保留。本重构使「F01+趋势闸 实盘决策 == 回测决策」成为架构保证 —— 推 F01 上 live 的决策正确性前提已夯实。
